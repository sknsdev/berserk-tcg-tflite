# 🃏 Проект распознавания карт Берсерк

TensorFlow Lite модель для распознавания карт из коллекционной карточной игры "Берсерк".

## 📋 Описание

Этот проект создает модель машинного обучения, способную распознавать карты из игры Берсерк по их изображениям. Модель определяет:

- **Сет карты**: s1, s2, s3, s4, s5, laar
- **Номер карты**: уникальный номер в рамках сета
- **Вариант карты**: normal, pf (полноформатная), alt (альтернативный арт)

## 🏗️ Архитектура

- **Базовая модель**: MobileNetV2 (оптимизирована для мобильных устройств)
- **Метод обучения**: Transfer Learning + Fine-tuning
- **Формат выходной модели**: TensorFlow Lite (.tflite)
- **Размер входного изображения**: 224x224 пикселей

## 📁 Структура проекта

```
berserk-tcg-tflite/
├── cards/                     # Папка с изображениями карт
│   ├── s1_1.webp
│   ├── s1_2.webp
│   ├── laar_1.webp
│   └── ...
├── main.py                    # Главный скрипт
├── data_preparation.py        # Подготовка данных
├── train_model.py            # Обучение модели
├── test_model.py             # Тестирование модели
├── requirements.txt          # Зависимости
└── README.md                 # Этот файл
```

## 🚀 Правильный запуск проекта

### 🎯 Проверка совместимости (рекомендуется)

**Сначала проверьте совместимость:**
```bash
python check_compatibility.py
```

Этот скрипт проверит:
- Версию Python и совместимость с TensorFlow
- Наличие необходимых файлов
- Данные в папке `cards`
- Установку TensorFlow
- Даст рекомендации по устранению проблем

### 🎯 Автоматическая настройка

**Самый простой способ:**
```bash
python setup.py
```

Этот скрипт автоматически:
- Проверит версию Python
- Создаст виртуальное окружение
- Установит все зависимости
- Проверит наличие данных
- Покажет инструкции для следующих шагов

### 🔧 Ручная настройка виртуального окружения

1. **Создание виртуального окружения:**
   ```bash
   # Windows
   python -m venv venv
   
   # Linux/macOS
   python3 -m venv venv
   ```

2. **Активация виртуального окружения:**
   ```bash
   # Windows
   venv\Scripts\activate
   
   # Linux/macOS
   source venv/bin/activate
   ```

3. **Установка зависимостей:**
   ```bash
   pip install -r requirements.txt
   ```

### Быстрый старт

4. **Проверка данных:**
   ```bash
   python main.py check
   ```

5. **Полный цикл обучения и тестирования:**
   ```bash
   python main.py full
   ```

   Эта команда выполнит:
   - Подготовку данных
   - Обучение модели
   - Тестирование модели

6. **Запуск веб-демонстрации:**
   ```bash
   python web_demo.py
   ```
   Откройте браузер и перейдите по адресу: http://localhost:5000

### Деактивация окружения
```bash
deactivate
```

### ⚠️ Важные замечания

- **Версия Python**: Рекомендуется Python 3.9-3.12. Для Python 3.13+ используется специальный файл `requirements-py313.txt`
- **Всегда используйте виртуальное окружение** для изоляции зависимостей проекта
- **Убедитесь, что папка `./cards` содержит изображения карт** в формате WebP
- **Для обучения модели требуется достаточно свободного места** (минимум 2-3 ГБ)
- **Процесс обучения может занять 30-60 минут** в зависимости от мощности компьютера
- **GPU ускорит обучение**, но не является обязательным требованием

## 🔧 Решение проблем с установкой

### Автоматическое исправление (рекомендуется)

Если возникают проблемы с установкой зависимостей:

```bash
python fix_installation.py
```

Этот скрипт автоматически:
- Попробует 5 различных методов установки
- Протестирует каждый метод
- Выберет рабочий вариант
- Даст рекомендации при неудаче

### Ручное решение проблем

Если автоматическое исправление не помогло:

1. **Python 3.13+**: Используйте специальный файл зависимостей:
   ```bash
   pip install -r requirements-py313.txt
   ```

2. **Альтернативная установка**:
   ```bash
   pip install tensorflow numpy pillow scikit-learn matplotlib opencv-python
   ```

3. **Только CPU версия TensorFlow**:
   ```bash
   pip install tensorflow-cpu numpy pillow scikit-learn matplotlib
   ```

4. **Использование conda**:
   ```bash
   conda install tensorflow numpy pillow scikit-learn matplotlib opencv
   ```

5. **Понижение версии Python**: Рекомендуется Python 3.9-3.12 для максимальной совместимости

## 📖 Подробное использование

### Доступные команды

```bash
# Показать информацию о проекте
python main.py info

# Проверить зависимости и данные
python main.py check

# Подготовить данные
python main.py prepare

# Обучить модель
python main.py train

# Протестировать модель
python main.py test

# Выполнить полный цикл
python main.py full
```

### Отдельные скрипты

```bash
# Подготовка данных
python data_preparation.py

# Обучение модели
python train_model.py

# Тестирование модели
python test_model.py
```

### Веб-демонстрация

```bash
python web_demo.py
```

Запускает веб-интерфейс для тестирования модели:
- Красивый интерфейс с drag-and-drop загрузкой изображений
- Мгновенное распознавание карт
- Отображение результатов с уровнем уверенности
- Поддержка всех популярных форматов изображений
- Доступен по адресу: http://localhost:5000

## 📊 Формат данных

### Названия файлов карт

Карты должны быть названы по формату:
```
{сет}_{номер}_{вариант}.{расширение}
```

Примеры:
- `s1_1.webp` - карта №1 из сета s1, обычная версия
- `s2_150.webp` - карта №150 из сета s2, обычная версия
- `laar_5.webp` - карта №5 из сета laar, обычная версия
- `s1_1_pf.webp` - карта №1 из сета s1, полноформатная версия
- `s1_1_alt.webp` - карта №1 из сета s1, альтернативный арт

### Поддерживаемые форматы изображений

- WebP (.webp)
- JPEG (.jpg, .jpeg)
- PNG (.png)

## 🎯 Результаты обучения

После обучения создаются следующие файлы:

- `berserk_card_model.tflite` - TensorFlow Lite модель (основная)
- `berserk_card_model.h5` - Keras модель (для дальнейшей работы)
- `model_info.json` - Информация о модели и энкодерах
- `label_encoders.json` - Энкодеры меток
- `cards_dataset.csv` - Подготовленный датасет
- `training_history.png` - График процесса обучения
- `test_results.png` - Визуализация результатов тестирования

## 📈 Процесс обучения

1. **Подготовка данных**:
   - Парсинг названий файлов
   - Создание меток классов
   - Кодирование категориальных переменных

2. **Обучение модели**:
   - Загрузка предобученной MobileNetV2
   - Transfer Learning (заморозка базовых слоев)
   - Fine-tuning (разморозка верхних слоев)
   - Аугментация данных

3. **Оптимизация**:
   - Конвертация в TensorFlow Lite
   - Квантизация для уменьшения размера

## 🧪 Тестирование

Скрипт тестирования предоставляет:

- **Автоматическое тестирование** на случайных изображениях
- **Визуализацию результатов** с отображением правильных/неправильных предсказаний
- **Интерактивный режим** для тестирования отдельных изображений
- **Метрики точности** и уверенности модели

## 🔧 Настройка параметров

### В файле `train_model.py`:

```python
# Размер изображения
input_shape = (224, 224, 3)

# Параметры обучения
epochs = 20
batch_size = 32
learning_rate = 0.0001

# Разделение данных
test_size = 0.3
validation_size = 0.15
```

### В файле `data_preparation.py`:

```python
# Путь к папке с картами
cards_dir = './cards'

# Размер изображения для предобработки
target_size = (224, 224)
```

## 📱 Использование модели

### Загрузка TensorFlow Lite модели

```python
import tensorflow as tf

# Загрузка модели
interpreter = tf.lite.Interpreter(model_path='berserk_card_model.tflite')
interpreter.allocate_tensors()

# Получение деталей входа и выхода
input_details = interpreter.get_input_details()
output_details = interpreter.get_output_details()
```

### Предсказание

```python
from test_model import BerserkCardPredictor

# Создание предиктора
predictor = BerserkCardPredictor()

# Предсказание для изображения
result = predictor.predict('path/to/card/image.webp')

print(f"Карта: {result['card_info']['card_id']}")
print(f"Уверенность: {result['confidence']:.4f}")
```

## 🐛 Решение проблем

### Ошибка "Модель не найдена"
```bash
# Убедитесь, что модель обучена
python main.py train
```

### Ошибка "Папка cards не найдена"
```bash
# Убедитесь, что папка cards существует и содержит изображения
ls ./cards/
```

### Проблемы с зависимостями
```bash
# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### Недостаточно памяти при обучении
```python
# Уменьшите batch_size в train_model.py
batch_size = 16  # или 8
```

## 📊 Ожидаемые результаты

- **Точность на тестовых данных**: 85-95% (зависит от качества и количества данных)
- **Размер TensorFlow Lite модели**: 5-15 MB
- **Время обучения**: 30-60 минут (зависит от оборудования)
- **Время инференса**: <100ms на современном CPU

## 🤝 Вклад в проект

Для улучшения проекта можно:

1. Добавить больше изображений карт
2. Улучшить аугментацию данных
3. Экспериментировать с другими архитектурами
4. Добавить поддержку новых сетов карт
5. Оптимизировать модель для конкретных устройств

## 📄 Лицензия

Этот проект создан в образовательных целях. Изображения карт принадлежат их правообладателям.

## 🆘 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Убедитесь, что все зависимости установлены
3. Проверьте формат названий файлов карт
4. Убедитесь, что в папке `cards` достаточно изображений

---

**Удачного обучения! 🚀**